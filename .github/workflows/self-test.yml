name: Self Test — AI Review
on:
  pull_request:
    branches: [main]

jobs:
  ai-review:
    uses: ghood82/ops-platform/.github/workflows/ai-code-review.yml@v1
    with:
      review_depth: 'light'
    secrets:
      ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
